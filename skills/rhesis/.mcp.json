{
  "mcpServers": {
    "rhesis": {
      "type": "http",
      "url": "${RHESIS_MCP_URL:-https://api.rhesis.ai/mcp/}",
      "headers": {
        "Authorization": "Bearer ${user_config.api_key:-${RHESIS_API_KEY}}"
      }
    }
  }
}
